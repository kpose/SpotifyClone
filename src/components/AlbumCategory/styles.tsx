import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 30,
    fontWeight: 'bold',
    //margin: 10,
  },

  container: {
    margin: 10,
  },
});

export default styles;
