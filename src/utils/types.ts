export type AlbumTypes = {
    id: string;
    imageUri: string;
    artistsHeadline: string;
    name: string;
    by: string;
    numberOfLikes: number

}

export type Song = {
    id: any;
    imageUri: string;
    title: string;
    artist: string;
}